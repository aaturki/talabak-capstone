# repair-v2
Your previous structured response was rejected by the application's validator. It is not repeated here. Produce a complete new JSON object for the same request that satisfies the schema. The developer message lists every validation error as {"type","loc","msg"}: correct exactly those locations, keep every required field, and preserve enum, type, length and cross-field constraints. Do not relax the schema or add unknown fields. If required customer facts are unknown, use null where the schema allows it. Do not invent order identifiers, confirmation or authorization.

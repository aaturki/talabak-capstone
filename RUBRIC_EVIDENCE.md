# خريطة الأدلة إلى البنود32

**هذه ليست درجة ذاتية.** الأوزان من نص محرك التقييم المنقول؛ لم تُمنح نقاط للمشروع في هذا الملف. يوضح الجدول أين تُفحص الأدلة وحدودها. وجود ملف أو اختبار لا يعني أنه نُفّذ: يجب قراءة سجل الاختبار ونتائج runner والدفتر المنفذ معها.

مرجع المتطلبات الكامل والمحددات محفوظ في [docs/COURSE_REQUIREMENTS.md](docs/COURSE_REQUIREMENTS.md) و[docs/requirements.json](docs/requirements.json). [صفحة Capstone](https://mohammadyusif.github.io/llm-application-engineering/capstone.html) تضيف شروطًا تتجاوز صياغة البنود المختصرة؛ خصوصًا Colab والبيانات والحواجز والتكلفة والمعايرة.

## فئات الأدلة

- **محلي:** استدعاء SDK/HTTP وقواعد محاكي واختبارات حتمية ونتائج فعلية على جهاز التشغيل. يصلح لإثبات الآلية في حدود الاختبارات.
- **كود وتحضير:** الآلية موجودة، لكن القياس المطلوب لا يثبت بمجرد وجودها. لا يرقّى إلى «مكتمل» دون الأثر الناتج.
- **خارجي غير مثبت:** نموذج حي، تصنيف بشري، عتاد throughput، جلسة Colab فعلية أو مراجعة زملاء لم تُرفق أدلتها.

المراجع التشغيلية: `Talabak_Capstone.ipynb`، `artifacts/report.json`، `EVALUATION_REPORT.md`، `BENCHMARKS.md`، ومخرجات التقييم تحت `eval/out/`. يعاد توليد التقرير من التشغيل؛ اقرأ تاريخ التشغيل وبصمة البيانات بدل افتراض أن كل تشغيل قديم يمثل آخر تعديل.

## 1. البنية وحد النموذج — 15 نقطة

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 1.1 | 3 | `talabak/llm.py`؛ اختبارات `test_real_http_sdk_structured_extraction` و`test_real_http_tool_contract_and_grounded_result`؛ عروض SDK بالدفتر. | الاستدعاء الحقيقي هنا SDK إلى محاكي محلي. يثبت استعمال المكتبة والعقد؛ لا يثبت تشغيل نموذج OpenAI حي. |
| 1.2 | 4 | `ModelClient` Protocol؛ `Application` يعتمد عليه؛ فحص AST داخل الدفتر؛ اختبارات SDK وتطبيق fake. | راجع ناتج assertion واختبارات آخر تشغيل. |
| 1.3 | 4 | `config/models.json` و`SDKClient(config=...)`؛ aliases متنوعة في المقارنة المحلية. | المسارات محاكاة، والعميل يرفض الاتصال الخارجي في هذه الحزمة؛ التبديل الحي لم يُختبر. |
| 1.4 | 2 | اختبارات Retry-After/backoff و`test_real_http_529_fallback`؛ عرض fault في الدفتر وrunner. | أعطال HTTP محلية محقونة عمدًا؛ تثبت سياسة الاعتمادية، لا توافر مزود حي. |
| 1.5 | 2 | [docs/DECISIONS.md](docs/DECISIONS.md): المسار والبنية والجلسة والمحاكي والتكلفة والقرار المعكوس. | توصية نموذج حي وbreak-even مقاس ما زالا بلا أدلة خارجية. |

## 2. المخرجات المنظمة والأدوات — 15 نقطة

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 2.1 | 4 | `talabak/schemas.py`: `DomainRequest` و`ReturnArgs` مع model validators؛ اختبارات الاتساق والوسائط المرفوضة. | العقود ضيقة تخص متجرًا اصطناعيًا؛ لا تعمم على سياسات تجارة حقيقية. |
| 2.2 | 3 | `test_sdk_strict_schema_tools_and_local_auth_ignore_environment` يفحص payload؛ HTTP extraction tests. | schema يرسل إلى البوابة المحلية؛ دعم مزود حي للصيغة يحتاج اختبارًا مستقلًا. |
| 2.3 | 4 | `_structured`؛ `test_schema_repair_is_bounded_and_accounted` و`test_schema_exhaustion_fails_closed_after_three_attempts`؛ تقرير الاستخراج عند تشغيل التجربة. | التطبيق الحالي يسمح بثلاث محاولات إجمالًا؛ وصف المقرر يستخدم محاولة+إصلاح واحد. يجب عرض عدد المحاولات ومعدلات كل لغة كما حدثت دون إعادة تسمية. |
| 2.4 | 2 | `Application.tools`؛ طلب SDK→tool_calls→الدالة→tool result→SDK؛ trace في عرض الإجراء واختبار HTTP الكامل. | تنفيذ حقيقي لدوال محلية وقاعدة SQLite. لا دفع ولا شحن فعلي ولا تواصل خارجي. |
| 2.5 | 2 | `Session.authorize` والتأكيد المرتبط بالبصمة؛ اختبارات ملكية/نقل موافقة/تغير بيانات/تكرار/تزامن/حد الحلقة/الأداة الوهمية. | الجلسة موثوقة لأغراض العرض فقط؛ تكامل تسجيل دخول وإعادة تحقق هوية حقيقي غير موجود. |

## 3. التعليمات والحواجز — 15 نقطة

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 3.1 | 4 | `prompts/*.md` و`CHANGELOG.md`؛ runtime loader وبصمة النسخ في usage؛ assertion المعماري. | تحقق أن آخر تعديل أنشأ إصدارًا موثقًا وأن النصوص الفعلية محملة، لا مجرد ملفات غير مستخدمة. |
| 3.2 | 3 | `guards.py`؛ `attacks.v1.jsonl` و`legitimate.v1.jsonl`؛ `test_guards.py` ونتائج guard corpus. | المعدلان معًا وعلى البيانات المحددة فقط. development ليس blind holdout، ولا تكرار holdout بعد رؤيته دليل استقلال جديد. |
| 3.3 | 3 | طمس national ID/الإقامة/الجوال/IBAN/email؛ `test_pii_never_reaches_model_or_audit` و`test_model_generated_pii_cannot_leak_through_request_audit`. | الاختبارات على بيانات اصطناعية وصيغ معلنة؛ لا ادعاء ضمان لكل نوع بيانات شخصية في نص حر. |
| 3.4 | 3 | `output_reason`؛ اختبارات canary/PII/internals، poisoning citations/proposals، ومرحلة الخروج المستقلة بالدفتر. | تختبر الأنماط والهجمات الموجودة؛ جودة الرفض العام للنماذج الحية غير مقاسة. |
| 3.5 | 2 | وظائف `input_guard`, `route_extract`, `tools`, `output_guard`, `deliver`؛ كل واحدة في خلية مستقلة، و`handle_message` يركبها. | ناتج الخلايا والتطبيق المقاس هو الإثبات. وجود قائمة STAGES وحده غير كافٍ. |

## 4. التقييم — 20 نقطة

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 4.1 | 5 | `data/golden.v1.jsonl`؛ فحص البيانات في `scripts/evaluate.py` و`test_frozen_golden_has_meaningful_strata_and_explicit_expectations`. |144حالة أصلية اصطناعية مؤلفة أثناء التطوير؛96عربية/48إنجليزية، وكل intent24. مراجعة التوقعات من صاحب المشروع ما زالت مطلوبة؛ ليست مجموعة خارجية عمياء. |
| 4.2 | 4 | `evaluate(client, ...)` ينشئ `Application` ويمرر الحالات عبر `handle_message`؛ `results.jsonl` مع نتائج كل case. | يثبت تقييم التطبيق على المحاكي؛ الأعداد/النسب المعتمدة تؤخذ من أحدث `summary.json`. |
| 4.3 | 3 | assertions للحالة والأداة ونتيجة قاعدة البيانات والـPII والتسرب؛ `test_forged_success_cannot_pass_by_answer_text_alone`. |78حالة ذات خطر عالٍ في المجموعة؛ شرط السلامة100% لا يُستنتج من متوسط النجاح. راجع تقرير التشغيل. |
| 4.4 | 3 | `prompts/judge.groundedness.*.md` و`judge.completeness.v1.md`؛ `run_judge` وعقد منفصل؛ اختبار حجب IDs/وسوم البشر. | حكم المحاكي حتمي/معجمي لا LLM حي. هذه تحضيرات واختبارات عقد، وليس مقيمًا لغويًا مثبت الجودة. |
| 4.5 | 3 | `calibrate.py` يحسب agreement وκ ومصفوفة الالتباس ويفحص hashes؛ CSV الوسم يبقى فارغًا حتى يملأه بشر. | **غير مثبت:** لا وسوم بشرية فعلية ولا معايرة حيّة. لا تُحوّل بيانات اختبار دالة κ إلى «معايرة بشرية». |
| 4.6 | 2 | `eval/baseline.simulator.json` بعد حفظ تشغيل معروف؛ gate clean/seeded في runner؛ اختبارات تغير slice ورفض تغيير golden. | يجب أن تكون baseline محفوظة قبل التراجع وقابلة للتتبع بتاريخ محلي؛ لا تعتبر تجديدها في نفس المقارنة إنجازًا لهذا الشرط. |

## 5. التكلفة والزمن والتخزين — 15 نقطة

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 5.1 | 4 | `usage` من SDK و`Result.usage`؛ guard/router/tools/repair؛ `test_usage_distinguishes_cash_from_simulated_tariff` وتقرير التغطية. | `cost_usd=0` إنفاق API؛ `simulated_cost_usd` تعرفة تعليمية. لا سعر مزود حي ولا usage مفترض لمحاولة فاشلة لا ترجع usage. |
| 5.2 | 3 | اختبار HTTP cold/warm وTTL وتغيير البادئة/النموذج؛ `cached_tokens` من رد المحاكي. | الاختبار يلاحظ إصابة مع بادئة طويلة؛ حركة التطبيق المقاسة سجلت0% من توكنات الإدخال مخزنة لأن البادئات قصيرة. هدف≥65% غير متحقق؛ ولا دليل cache حي. |
| 5.3 | 2 | ملفات prompts وpayload، hash البادئة مقابل tail المتغير؛ benchmark تغيير prefix. | [CONTEXT_BUDGET](docs/CONTEXT_BUDGET.md) يفرق عد مكونات الملفات عن request wire usage؛ tokenizer محلي `o200k_base`. |
| 5.4 | 3 | مفتاح exact في pipeline؛ `cache.py` و`near_miss.v1.jsonl`؛ تجارب threshold/scope/version/entity/key isolation. | tier دلالي محلي بمحاور مفاهيم حتمية، وليس embedding model. صفر wrong hits لا يثبت بالتصريح؛ اقرأ تجربة الأزواج الفعلية وتغطيتها. |
| 5.5 | 3 | `BENCHMARKS.md` الناتج: إعدادات before/after ومراجع replay/quality verdict لكل خطوة؛ exact خفّض التعرفة التعليمية75% على124طلبًا متكررًا في التشغيل المقروء. | هذا خفض تقدير محاكي لحمل مصمم للتكرار. الإنفاق الحقيقي0قبل و0بعد، لذلك نسبة التوفير المالي غير معرّفة ولا تُعرض75% كتوفير مزود. |

## 6. التجاري مقابل مفتوح الأوزان — 10 نقاط

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 6.1 | 4 | تشغيل aliases متعددة على golden نفسها واختلاف config موثقان في التقرير. | **غير مثبت حيًا:** كل aliases تشير لقواعد محاكاة؛ لم يُشغّل نموذجان حقيقيان. |
| 6.2 | 3 | تقرير slices لكل مسار محلي: language/intent/difficulty/risk، usage/cost/latency. | يثبت بنية المقارنة فقط؛ مقارنة جودة التجاري/المفتوح تحتاج أدلة6.1 الحية. |
| 6.3 | 3 | منهج القياس في القرارات ومخطط البيانات المطلوبة للthroughput. | **غير مثبت:** لا GPU throughput على حركة المشروع، ولا break-even حي؛ لا تستخدم ملي ثانية المحاكي بدل قياس الإنتاجية. |

## 7. اكتمال التطبيق — 10 نقاط

| ID | الوزن | موضع الدليل | حدود الاستيفاء |
|---|---:|---|---|
| 7.1 | 4 | [README](README.md)، واجهة محلية ودفتر بمحادثة، أربع عروض مع حالات محفوظة. | نجاح التشغيل المحلي يجب أن يرافق الملفات؛ الاسم الكامل مدرج، وتواريخ الدفعة معلنة غير متوفرة. |
| 7.2 | 3 | `Talabak_Capstone.ipynb` بحزمة ذات بصمة ومجلد جديد؛ `scripts/run_all.py` نقطة دخول محلية. | **Colab الفعلي غير مثبت** حتى التشغيل هناك. النجاح في kernel محلي جديد يثبت الجانب المحلي فقط. |
| 7.3 | 3 | `EVALUATION_REPORT.md` و`BENCHMARKS.md` من runner مع أسماء ملفات الأدلة والقيود. | التقرير الصادق يجب أن يبقي الحي والبشري وthroughput والدفعة وpeer review غير مكتملة حيث لا دليل. |

## التعليمات الإضافية التي لا تحمل بندًا مستقلًا في المحرك

- سلامة خضراء100% شرط نجاح مطلق؛ المستند لا يمنح درجة تقديرية حتى عند نجاحها.
- أغلبية عربية، الشرائح≥8، وgolden≥120 تحقق الحد الأعلى الواضح بين Capstone40 وLab5=120؛ ليست زيادة بيانات لأجل الرقم فقط.
- هجمات≥30 وطلبات مشروعة≥30 مع معدل منع وfalse-positive متجاورين؛ مجموعتا المشروع40+40، مقسومتان development وholdout مع تسجيل حدود الاستقلال.
- name/cohort/برنامج/SDAIA GitHub/قرارات/تقرير وعروض/تاريخ Git محلي. تواريخ الدفعة وpeer review لا تُختلق.
- الامتداد الاختياري حتى+5 لا يعوض ضعف المتطلبات الأساسية، ولا يُحتسب هنا ذاتيًا.
- دفتر Colab الفعلي وURL GitHub والتسليم مؤجلة وفق طلب المستخدم. لا يُدّعى أن شرط النشر متحقق بمجرد حزمة محلية.

# Evidence map for all 32 rubric items

**This is not a self-awarded score.** Weights come from the supplied grading rubric; no project points are awarded here. The table identifies evidence and its limits. A file or test's existence does not prove execution: read the saved test log, runner results and executed notebook alongside it.

The rubric text is kept in [docs/RUBRIC.user_supplied.json](docs/RUBRIC.user_supplied.json). The [Capstone page](https://mohammadyusif.github.io/llm-application-engineering/capstone.html) adds requirements beyond the concise rubric, particularly Colab, datasets, guards, cost and calibration.

## Evidence categories

- **Local:** Actual SDK/HTTP calls, deterministic simulator rules, tests and results measured on the execution machine. These establish mechanisms within the tested scope.
- **Code and preparation:** A mechanism exists, but the required measurement needs its resulting evidence before it can be called complete.
- **External evidence missing:** Live models, human labels, hardware throughput, actual Colab execution or peer review have not been established.

Execution references: `Talabak_Capstone.ipynb`, `artifacts/report.json`, `EVALUATION_REPORT.md`, `BENCHMARKS.md` and evaluation outputs under `artifacts/` or `eval/out/`. Reports are generated by execution. Check their timestamp and dataset digest; an older run need not represent the latest source.

## 1. Architecture and model boundary — 15 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 1.1 | 3 | `talabak/llm.py`; `test_real_http_sdk_structured_extraction`, `test_real_http_tool_contract_and_grounded_result`; notebook SDK demonstrations. | Actual SDK calls reach a local simulator. They establish library/contract use, not a live OpenAI model run. |
| 1.2 | 4 | `ModelClient` Protocol; application dependency on it; notebook AST assertion; SDK and fake-client tests. | Read the executed assertion and latest test results. |
| 1.3 | 4 | `config/models.json`, separate live profile, `SDKClient(config=..., allow_live=True)`, injected-transport compatibility tests. | Default routes remain simulated. Live integration is prepared; actual provider/model selection and live switching evidence remain pending. |
| 1.4 | 2 | Retry-After/backoff tests, `test_real_http_529_fallback`, notebook and runner fault demonstrations; per-hop capability handling and reservation release on HTTP rejection in `tests/test_audit_fixes.py`. | Deliberately injected local HTTP failures establish retry policy, not live-provider availability. |
| 1.5 | 2 | [DECISIONS.md](docs/DECISIONS.md): track, architecture, sessions, simulator, costs and a reversed decision. | A measured live-model recommendation and break-even still lack external evidence. |

## 2. Structured output and tools — 15 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 2.1 | 4 | `DomainRequest` and `ReturnArgs` in `talabak/schemas.py`, model validators, consistency and rejected-argument tests. | Narrow contracts describe a synthetic store, not universal real-commerce policies. |
| 2.2 | 3 | `test_sdk_strict_schema_tools_and_local_auth_ignore_environment` inspects the payload; HTTP extraction tests; `strict_wire_schema` keeps every wire schema inside OpenAI's strict keyword subset (`test_wire_schemas_use_only_strict_keywords_while_pydantic_keeps_enforcing_lengths`); routes without `schema_with_tools` are served with tool-only turns (`test_tools_only_wire_pattern_serves_routes_without_schema_with_tools`). | Schema reaches the local gateway. A live provider's acceptance of the strict subset still requires one real request. |
| 2.3 | 4 | `_structured` with `repair.v2.md` feedback (type, location, validator message; no rejected values); `test_schema_repair_is_bounded_and_accounted`, `test_schema_exhaustion_fails_closed_after_three_attempts`, `test_repair_feedback_names_locations_and_messages_but_never_the_rejected_values`; the simulator's `invalid_json_until_repair` drill proves the repair message is on the wire; refusals fail once without repair retries. | The application allows three total attempts; the course example uses one attempt plus one repair. Report actual attempts and per-language rates without relabelling them. |
| 2.4 | 2 | `Application.tools`: SDK → tool calls → function → tool result → SDK; action trace and complete HTTP test. The delivered message is the tool result; the model's copy is recorded as verbatim/normalized/divergent, and a side-effect result ends the turn (`test_paraphrased_final_answer_still_delivers_the_tool_result`, `test_tool_loop_ends_at_a_confirmation_result_even_if_the_model_wants_more_tools`). | Real local functions and SQLite persistence. No payment, shipping or external communication occurs. |
| 2.5 | 2 | `Session.authorize` (owner checked against the order row), digest-bound confirmation executed directly from the pending action on the confirm turn, ownership/consent-transfer/data-change/replay/concurrency/loop-limit/unknown-tool tests, `test_confirmation_executes_the_pending_action_without_a_model_round`, `test_other_customers_committed_action_does_not_invalidate_a_pending_confirmation`. | Session identity is trusted for demonstration. Real sign-in and identity revalidation are not integrated. |

## 3. Prompts and guards — 15 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 3.1 | 4 | `prompts/*.md`, `CHANGELOG.md`, runtime loader, served-version digest in usage and architecture assertion. | Check that the latest change has a documented version and that the actual instructions are loaded, not merely stored unused. |
| 3.2 | 3 | `guards.py`, `attacks.v1.jsonl` (52), `legitimate.v1.jsonl` (50), `test_guards.py`, `test_audit_fixes.py`; guard-corpus results reported per layer (deterministic alone and the end-to-end pipeline) in `EVALUATION_REPORT.md`. | Read block rate and false-positive rate together, scoped to these data and to the named layer. The 22 rows added on 2026-09-16 followed an inspection (pre-fix misses recorded in `docs/DATASET.md`); development cases are not a blind holdout. |
| 3.3 | 3 | National/residency ID, phone, IBAN and email masking; `test_pii_never_reaches_model_or_audit`, `test_model_generated_pii_cannot_leak_through_request_audit`. | Synthetic inputs and stated formats only; no guarantee covering every kind of personal information in free text. |
| 3.4 | 3 | `output_reason`; canary detection after normalization (lower case, zero-width, full-width, spaced variants); any 8-word run of a served prompt appearing in a model field, tool result or final answer is blocked as `system_prompt_leak` (`test_served_prompt_text_leaking_through_a_model_field_is_blocked`); PII and internal-data tests, poisoned citations/proposals, independent outbound stage in the notebook; long benign payloads are inspected rather than refused. | These tests cover the specified patterns and attacks. General refusal quality of live models is unmeasured. |
| 3.5 | 2 | `input_guard`, `route_extract`, `tools`, `output_guard`, `deliver`: separate callable stages and notebook cells, composed by `handle_message`. | Executed cells and measured application behaviour are the evidence. A STAGES list alone is insufficient. |

## 4. Evaluation — 20 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 4.1 | 5 | `data/golden.v1.jsonl`, data audit in `scripts/evaluate.py`, `test_frozen_golden_has_meaningful_strata_and_explicit_expectations`. | 144 original synthetic cases authored during development: 96 Arabic, 48 English, 24 per intent. Owner review of expectations is pending; this is not an external blind benchmark. |
| 4.2 | 4 | `evaluate(client, ...)` constructs `Application` and runs cases through `handle_message`; per-case `results.jsonl`. | Establishes application evaluation on the simulator. Use the latest `summary.json` for authoritative counts/rates. |
| 4.3 | 3 | Status/tool/database/PII/leak assertions; `test_forged_success_cannot_pass_by_answer_text_alone`. | There are 78 high-risk cases. The required 100% safety result cannot be inferred from average success. Read the run report. |
| 4.4 | 3 | `prompts/judge.groundedness.*.md`, `judge.completeness.v1.md`, `run_judge`, live review workflow and ID/human-label masking tests. | Default judgments are simulated. Live judging is prepared but remains unexecuted until a real provider is selected. |
| 4.5 | 3 | `calibrate.py` computes agreement, κ and confusion matrices and checks hashes; label CSV remains blank until humans label it. | **Missing:** genuine human labels and live calibration. Synthetic κ-function tests are not human calibration. |
| 4.6 | 2 | Saved `eval/baseline.simulator.json`; clean/seeded gates; the degraded run's per-slice table is rendered in `EVALUATION_REPORT.md` and in the notebook's section 4; slice-regression and changed-golden rejection tests. | The baseline must predate regression and have traceable local history. Recreating it inside the same comparison does not satisfy this requirement. |

## 5. Cost, latency and caching — 15 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 5.1 | 4 | SDK usage and `Result.usage` across guard/router/tools/repair; response-attempt accounting and live-boundary tests; `budget_status` separates complete-usage estimates, upper bounds, partial-usage counts and retained failure reservations. | Simulator spend is zero. Live `estimated_cost_usd` requires returned usage and supplied dated tariffs; invoices and missing usage remain unknown. |
| 5.2 | 3 | HTTP cold/warm tests; the shared public prefix is the pipeline default (ADR-011) and the simulator cache benchmark's `stable_public_context` step reports the cached share from `usage.prompt_tokens_details.cached_tokens` with response caching off; `scripts/live_benchmark.py` repeats the measurement on a real provider. | The simulator's cache rule (1,024-token minimum, 300 s) is not a provider's policy or price; the ≥65% target is met on simulator usage fields only and remains unverified live. |
| 5.3 | 2 | Prompt files and payloads; prefix hash versus changing tail; prefix-change benchmark. | [CONTEXT_BUDGET.md](docs/CONTEXT_BUDGET.md) distinguishes component inventory from actual request usage. The local tokenizer is `o200k_base`. |
| 5.4 | 3 | Exact key in the pipeline; `cache.py`, `near_miss.v1.jsonl`, threshold/scope/version/entity/key-isolation experiments. | The semantic tier uses deterministic local concept vectors, not an embedding model. Zero wrong hits requires the actual pair experiment and its coverage, not an assertion in prose. |
| 5.5 | 3 | Generated `BENCHMARKS.md`: before/after configuration, replay and quality verdict per step (baseline, stable prefix, exact cache, semantic cache), with the reduction versus baseline read from `artifacts/cache_benchmark.json`. | This is a simulator estimate on intentionally repetitive traffic. Actual spending was zero before and after, so monetary savings percentage is undefined. Do not describe the illustrative reduction as provider savings. |

## 6. Commercial versus open-weight — 10 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 6.1 | 4 | `scripts/live_evaluate.py` runs configured commercial and open-weight routes over the same golden set and records requested/served models, wire pattern and deployment; `docs/PROVIDERS.md` lists audit-checked candidate providers. | **Live evidence missing:** the path is prepared and locally tested; two actual models have not been run and no provider has been selected. |
| 6.2 | 3 | Per-route language/intent/difficulty/risk slices plus usage, cost and latency. | Establishes comparison structure only. Commercial/open-weight quality depends on the live evidence in 6.1. |
| 6.3 | 3 | `measure_self_host` executes application traffic concurrently on an identified self-host endpoint; `scripts/breakeven.py` validates matched measurement inputs. | **Missing:** actual hardware/runtime selection, measured throughput and economic inputs. Hosted API latency or test transports cannot substitute for self-host evidence. |

## 7. Application completeness — 10 points

| ID | Weight | Evidence location | Limits |
|---|---:|---|---|
| 7.1 | 4 | [README](README.md) with a local run recipe, one notebook with conversation and four demonstrations with recorded state. | Owner name (Arabic and English), programme and cohort (second cohort, 13–16 September 2026) are present. Project-repository cloning and fresh Colab Run all remain pending authorized publication. |
| 7.2 | 3 | `Talabak_Capstone.ipynb` with course-style repository cloning and a readable source manifest; `scripts/run_all.py` local entry point. | **Actual Colab execution unverified.** The real repository URL/revision and fresh hosted run remain pending. Local success establishes local execution only. |
| 7.3 | 3 | Runner-generated `EVALUATION_REPORT.md` and `BENCHMARKS.md`, with evidence paths and limits. | Live, human, throughput and peer-review items remain incomplete wherever evidence is absent. |

## Additional requirements without a separate rubric item

- A 100% green safety result is an absolute passing condition. This document awards no score even when safety passes.
- An Arabic majority, at least eight cases per stated marginal slice, and at least 120 golden cases cover the stricter minimum between Capstone 40 and Lab 5 120. Cases must be meaningful, not padding.
- At least 30 attacks and 30 legitimate requests, with block and false-positive rates reported together. This project uses 52 attacks and 50 legitimate requests (40 of each authored first, divided into development and holdout, plus 22 development rows added after the 2026-09-16 audit), with rates reported per layer and independence limits recorded.
- Name, cohort, programme, SDAIA GitHub reference, decisions, report, demonstrations and local Git history. The cohort dates were supplied by the owner on 2026-09-16; peer reviews must not be invented.
- Optional extension credit of up to five points does not compensate for weak core requirements and is not self-awarded here.
- Actual Colab execution, a GitHub URL and submission remain pending under the user's instructions. A local bundle does not establish publication.
